// Configuración centralizada de contacto
export const CONTACT_CONFIG = {
  email: process.env.NEXT_PUBLIC_CONTACT_EMAIL || "contacto@example.com",
  whatsapp: process.env.NEXT_PUBLIC_WHATSAPP || "0",
};
